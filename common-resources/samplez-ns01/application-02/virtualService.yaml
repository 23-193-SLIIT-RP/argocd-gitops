apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: samplez
spec:
  http:
  - match:
    - uri:
        prefix: /application-02
    route:
    - destination:
        host: application-02.samplez-ns01.svc.cluster.local
        port:
          number: 8080